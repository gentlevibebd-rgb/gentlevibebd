import ContactClient from "./ContactClient";
import "./contact.css";

export const metadata = {
  title: "Contact Us — Gentle Vibe BD",
  description: "Get in touch with Gentle Vibe BD — questions about orders, products or anything else. We usually reply within a few hours.",
};

export default function ContactPage() {
  return <ContactClient />;
}
